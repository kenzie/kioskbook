#!/bin/sh
# Kiosk App Startup Script

cd /opt/kiosk-app

# Check if app is built
if [ -d "dist" ]; then
    # Serve built Vue.js app
    npx http-server dist -p 3000 -a 0.0.0.0
else
    # Fallback: serve current directory
    npx http-server . -p 3000 -a 0.0.0.0
fi
